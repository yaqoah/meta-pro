# ---------------------------------------------------------------------------
# Meta-Pro frontend — Vite / React / TypeScript
# ---------------------------------------------------------------------------

# --- Logs -----------------------------------------------------------------
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# --- Dependencies & build output ------------------------------------------
node_modules
dist
dist-ssr
coverage
*.local

# --- Environment files ----------------------------------------------------
# Local secrets / overrides. Template files (e.g. `.env.example`) stay tracked.
.env
.env.local
.env.*.local
!.env.example

# --- TypeScript -----------------------------------------------------------
*.tsbuildinfo
tsconfig.tsbuildinfo

# --- Lint / tool caches ---------------------------------------------------
.eslintcache
.vite/

# --- Editor directories and files -----------------------------------------
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# --- OS -------------------------------------------------------------------
Thumbs.db
