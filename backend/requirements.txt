fastapi
uvicorn[standard]
pydantic>=2.0
pydantic-settings
typing-extensions
instructor
litellm
langgraph
# Optional checkpointer extras — the backend auto-falls back to in-memory
# MemorySaver when neither is installed, so neither is required to run.
langgraph-checkpoint-postgres
langgraph-checkpoint-sqlite
tenacity
redis
psycopg[binary]
psycopg-pool
python-multipart
# Media transcription — Mistral Voxtral via the official SDK. The Groq SDK
# is intentionally absent: Groq is fully optional (emergency LLM fallback
# only) and requires no installed package.
mistralai>=2.0
python-dotenv
